# Core Dependencies - Required to run the application
# Windows 11 only

# UI Framework
flet==0.25.2

# Windows API (Windows only)
pywin32==306

# System utilities
psutil==5.9.8

# .NET interop for LibreHardwareMonitor (Windows only)
pythonnet==3.0.5

# Optional: Log encryption (install separately if needed)
# cryptography==41.0.7

# For development/testing dependencies, see requirements-dev.txt
